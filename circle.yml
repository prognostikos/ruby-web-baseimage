machine:
  services:
    - docker

test:
  override:
    - make

deployment:
  prod:
    branch: master
      - docker login -e $DOCKER_EMAIL -u $DOCKER_USER -p $DOCKER_PASS
      - docker push prognostikos/ruby-web-baseimage
